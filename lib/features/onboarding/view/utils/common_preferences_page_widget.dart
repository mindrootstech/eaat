import 'package:flutter/material.dart';

class CommonPreferencesPageWidget extends StatelessWidget {
  const CommonPreferencesPageWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(children: []);
  }
}
